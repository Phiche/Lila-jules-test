package com.example.lilaism;

import java.util.function.Function;

// Assuming a Pair class or similar tuple construct.
// Using the Pair class defined previously in EmailAddress.java for now,
// though a more generic one would be better.
// Or Java records if appropriate for specific tuple uses.

public class TupleUtils {
    // Example for a Pair, would need to be adapted if different tuple types are used.
    // public static <A, B, C> Pair<A, C> map2(Pair<A, B> self, Function<B, C> f) {
    //    return new Pair<>(self.getKey(), f.apply(self.getValue()));
    // }
}
